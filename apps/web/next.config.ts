import type { NextConfig } from "next";

// Placeholder for Phase 1. Replaced by the real Phase 2 config (Tailwind,
// Vercel deployment, etc.) when the browse UI lands.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
