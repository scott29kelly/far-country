{
  "name": "far-country-web",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 placeholder: loads the exported canonical.json and renders one entity (New Jerusalem). Replaced by the real browse UI in Phase 2.",
  "scripts": {
    "dev": "next dev -p 3030",
    "build": "next build",
    "start": "next start -p 3030",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
