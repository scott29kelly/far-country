# FC-0009 mutation battery — predictions (pre-registered 2026-08-24)

This file is the pre-registration required by practice 4 of the instrument
doctrine: **the predicted verdict for every mutation is written down and
committed BEFORE any mutation is applied.** A prediction changed after the
fact is worthless; a wrong prediction recorded honestly is a finding.

Debt being paid: FC-0009. The 18-member battery (tools/battery.ts) has never
been shown to FAIL on purpose. "18/18 PASS" is only evidence if a break makes
it say something else. Seed list: the per-tool mutation table at the bottom of
tools/AUDIT-2026-08-19.md.

Tree under test: `claude/far-country-post-ga3-defects-9f1a01` at `cc5a86e`
(= `main`). Probe sources are identical on the post-GA-3 defect branch; FC-0019
touched src/render/PostStack.ts and FC-0020 touched src/world/TerrainTiles.ts,
neither of which any battery member reads.

## Protocol

1. Establish a clean baseline: `npm run battery` with :5173 up. Must be 18/18
   PASS. Anything else and the mutation run is void.
2. Apply exactly ONE mutation to engine source (never to a probe).
3. Run the target member. Record its verdict and the failing check IDs.
4. Run the remaining CPU members (`npm run battery -- --cpu`) to record
   cross-coverage — which OTHER members noticed. Live members are re-run only
   where a live effect is plausible, because each costs a browser boot.
5. `git checkout --` the mutated file. Confirm the target member is PASS again
   before the next mutation, so no mutation leaks into the next.

Rules of scoring:

- **CAUGHT** — the target member FAILS, on the predicted check ID.
- **CAUGHT, WRONG CHECK** — the target member FAILS but a different check
  fires. The instrument works; the prediction was wrong. Both get recorded.
- **BLIND** — the target member PASSES. A blind spot, and the reason it is
  blind goes in DEFECTS.md as a new entry.
- **UNMEASURED** — exit 2. Never counted as a pass (check.ts three-verdict
  contract).

## Predictions

`sy=1` style notation means the literal being changed. "Also fails" is the
predicted cross-coverage; "none" is a real prediction and can be wrong.

| # | Member | Mutation (engine source only) | Predicted verdict | Predicted check | Predicted also-fails |
|---|---|---|---|---|---|
| 1 | stages | `src/nj/stages.ts` `parseStages()` returns an empty `Set` for a null param | FAIL | A1 | stages-live (not a member) |
| 2 | navigation | `src/core/FlyCamera.ts` `setFlySpeed()` — drop the 2000 m/s clamp | FAIL | A4 | none |
| 3 | arrival | `src/core/Easing.ts` `easeInOutCubic` — return `k*2` for `k>0.5` (breaks monotonicity) | FAIL | A2 | bootrite (not a member) |
| 4 | walkfling | `src/nj/RiverOfLife.ts` `riverSurfaceLocalY()` — ignore the `maxSurfaceY` cap | FAIL | A2, D, E | walkfling-live (not a member) |
| 5 | wallcollide | `src/nj/cityModel.ts` `foundationCourseSpans()` — delete the gate notch | FAIL | P4, W2 | none |
| 6 | cityfloors | `src/nj/cityModel.ts` `TIER_GLASS_PROUD` → 0 | FAIL | F3 | wallcollide (shares the tier table) |
| 7 | templecollide | `src/nj/templeModel.ts` `INTERP.stepRise` → 0 | FAIL | T11, T13 | framings (T_COURT_Y derives from stepRise in reviewFramings.ts) |
| 8 | dwellingscollide | `src/nj/Dwellings.ts` line ~510 — court well `sy` 1 → 4 (a full-height wall, not a step-over) | FAIL | D7 | none |
| 9 | ascent | `src/nj/ascentModel.ts` `SLAB_PROUD` 2.5 → 3.5 | FAIL | A7 | none |
| 10 | population | `src/nj/populationModel.ts` — move one assembly 1 unit into a gate corridor | FAIL | A3 | crowd (reads the same tables) |
| 11 | crowd | `src/nj/figureModel.ts` — raise one archetype's LOD0 triangle output past `CROWD_LOD.tris0Max` (6500) | FAIL | B1 | none |
| 12 | framings | `src/nj/reviewFramings.ts` — point `plaza-axis` lookAt outside `CITY_HALF` | FAIL | E1 | none |
| 13 | entitypick | `src/nj/cityModel.ts` `FOUNDATION_GEMS` — swap two gem names | FAIL | A2 | visualkey (not a member) |
| 14 | gamepad | `src/core/GamepadInput.ts` — radial deadzone → 0 | FAIL | A | gamepad-live |
| 15 | gamepad-live | `src/core/GamepadInput.ts` — break the default `navigator` source closure | FAIL | L2, L3 | gamepad (CPU) — uncertain, the CPU probe may inject its own source |
| 16 | mousesteer | mouse-steer pitch sign inverted | FAIL | check 4 | none |
| 17 | wildwater | `src/nj/wildRing.ts` — raise the west-lake level | **PASS (predicted BLIND)** | — | none |
| 18 | rimfalls | `src/nj/RimFalls.ts` `CLUSTER_M` 260 → 130 (splits the along-rim dedup lattice) | FAIL | R1 or R2 | wildwater — no (see #17) |

### Standing predictions that are the point of the exercise

- **#17 wildwater is predicted to PASS.** Audit finding F1 says the probe emits
  no verdict at all, so it cannot fail. If it passes under a deliberate
  hydrology break, F1 is confirmed by experiment rather than by reading, and
  wildwater's membership in the battery is currently decorative. That is the
  single most valuable result available from this run.
- **#7's cross-coverage is a mirror-pin test (audit F6).** If framings does NOT
  fail when stepRise changes, the two are more decoupled than the source
  suggests. If templecollide passes, the probe is deriving its expectation from
  the same constant it is checking, which is the self-referential pair the
  audit flagged.
- **#18 has the softest prediction** because rimfalls asserts an emergent site
  set. R1 (exactly three sites) and R2 (positions within 50 m) are both
  plausible firing points; R3 (drop band) is predicted NOT to fire, since the
  cliff geometry is untouched.

### Not covered by this run

`bootui` (audit F5 — predicted unfailable) and `shoot.ts` (audit F3 — an
all-black canvas passed before the frame gate) are in the seed list but are
**not battery members**, so they are out of scope here. They stay owed.
